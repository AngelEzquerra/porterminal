server:
  # Use 127.0.0.1 for local-only access, 0.0.0.0 only with Cloudflare tunnel
  host: "127.0.0.1"
  port: 8000

terminal:
  # Shells are auto-detected based on your platform
  # Uncomment below to override:
  # default_shell: "bash"
  # shells:
  #   - name: Custom Shell
  #     id: custom
  #     command: /path/to/shell
  #     args: []
  cols: 120
  rows: 30

buttons: []

cloudflare:
  team_domain: ""
  access_aud: ""
